{% extends '@EMSCH/template/variables.twig' %}

{% if emsLink.contentType|default(null) == 'structure' %}
    {% set canonicalUrls = {
        fr: 'toto',
        nl: 'toto',
    } %}
{% elseif emsLink.contentType|default(null) == 'page' %}

    {% set canonicalUrls = {
        fr: 'toto',
        nl: 'toto',
    } %}
{% endif %}


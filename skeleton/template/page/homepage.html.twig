{% extends '@EMSCH/template/parent/homepage.html.twig' %}
{% trans_default_domain trans_default_domain %}

{% block body %}
    <main role="main" id="main-content">
        {{ block('homeHeader') }}
        <div class="container">
            {{ block('news') }}
            {{ block('themes') }}
            {{ block('actions') }}
        </div>
    </main>
{% endblock %}